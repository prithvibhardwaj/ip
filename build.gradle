plugins {
    id 'java'
    id 'application'
    id 'checkstyle'
}

repositories {
    mavenCentral()
}

dependencies {
    testImplementation group: 'org.junit.jupiter', name: 'junit-jupiter-api', version: '5.10.0'
    testRuntimeOnly group: 'org.junit.jupiter', name: 'junit-jupiter-engine', version: '5.10.0'
}

test {
    useJUnitPlatform()
}

application {
    mainClass = 'krypto.Krypto' // CHANGED FROM duke.Krypto TO krypto.Krypto
}

checkstyle {
    toolVersion = '10.12.4'
}